import './index.scss';

export default function Pause(): React.ReactNode {
    return <view className="pause">
        <view className="content">
            <h1 className="title">PAUSED</h1>
        </view>
    </view>;
}